<%= form_with(model: [:admins, company_value]) do |form| %>
  <% if company_value.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(companyvalue.errors.count, "errors")%> prohibited this Company Value from being saved:</h2>

    <ul>
      <% company_value.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <%= form.submit %>

<% end %>
